# NOTE: data/ is *not* gitignored. It's a build output (regenerated by
# build.py from the source JSON), but GitHub Pages serves what's in the
# repo, so we commit it. Re-run `python3 build.py` and commit the diff
# whenever the source changes.

# OS
.DS_Store
Thumbs.db

# Editors
.idea/
.vscode/
*.swp

# Python
__pycache__/
*.pyc
.venv/
venv/

# Local server logs
*.log
